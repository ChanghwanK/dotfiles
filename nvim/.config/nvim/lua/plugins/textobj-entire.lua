return {
    "kana/vim-textobj-entire",
    dependencies = { "kana/vim-textobj-user" },
    -- ae: 파일 전체 (leading/trailing 빈 줄 포함)
    -- ie: 파일 전체 (leading/trailing 빈 줄 제외)
    -- 사용 예: Vae (전체 선택), dae (전체 삭제), yae (전체 복사)
}
